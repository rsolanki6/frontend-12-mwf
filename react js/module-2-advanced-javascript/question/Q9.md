# What is type coercion?

**Type coercion** is the automatic or implicit conversion of one data type to another by a programming language.

It usually happens when different data types are used together in an operation.

### Example
```javascript
"5" + 2 
```